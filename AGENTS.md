# AGENTS.md

This is the home directory workspace at `/home/tiger/`. Primary instruction source: [`.openclaw/workspace/AGENTS.md`](.openclaw/workspace/AGENTS.md).

## Repo Structure

- `.openclaw/` — main tracked workspace (projects, skills, memories)
- `scripts/` — utility scripts
- `python-venv/` — local Python venv
- Hidden dirs (`.git/`, `.claude/`, `.gstack/`, etc.) — tool configs

## Key Facts

- **Location**: `/home/tiger/` is the OpenClaw workspace root
- **Tracked content**: `.openclaw/workspace/` is git-tracked (see `.gitignore`)
- **Behavioral guidelines**: See root [`CLAUDE.md`](CLAUDE.md) for coding guardrails
- **gstack skills**: Use gstack for web browsing (`/browse`), QA (`/qa`), debugging (`/investigate`), etc.

## Commands

No standard build/test commands — this is a personal workspace, not a software project.

---

For detailed session startup and workflow rules, see `.openclaw/workspace/AGENTS.md`.